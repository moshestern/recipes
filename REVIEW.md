# Audit + Code Review – Recipe Site v6

## תוצאה כללית

ב־v6 יושמו ארבעת השיפורים הראשונים שהוגדרו למצב הבישול. האתר נשאר סטטי ופשוט, וה־JSON ממשיך להיות מקור הנתונים היחיד.

## מה השתנה

### 1. זמני מתכון מסכמים

לכל 25 המתכונים יש עכשיו `times` מסכם. לפי הצורך מופיעים:
- `prep_minutes`
- `cook_minutes`
- `bake_minutes`
- `rest_minutes`
- `chill_minutes`

בכרטיסים מוצגים זמני ההכנה והבישול/אפייה בלבד כדי לא להעמיס במובייל; בעמוד המתכון מוצגים גם זמני מנוחה וקירור.

הזמנים המסכמים נבנו מהגרסה הסופית של הוראות ההכנה. כאשר המתכון כולל טווח, נשמר טווח ולא הומר למספר יחיד.

### 2. טיימר רק כאשר הוא באמת שימושי

האתר כבר לא מציג טיימר אוטומטית לכל שלב של 10 דקות ומעלה. במקום זה השלב צריך לכלול במפורש:

```json
"suggest_timer": true
```

כך טיגון פעיל, השחמה ופעולות שדורשות תשומת לב לא מעמיסות על המסך בכפתורי טיימר.

כרגע יש 39 שלבים שבהם מוצע טיימר מתוך 80 שלבים שיש בהם מידע על משך זמן.

### 3. טווחי זמן

נוסף `timer_options`. בטווחים כמו 35–45 דקות ברירת המחדל היא הקצה התחתון, כדי לבדוק מוכנות לפני שממשיכים.

לדוגמה:

```json
{
  "timer_minutes": 35,
  "timer_options": [35, 45],
  "suggest_timer": true
}
```

ב־UI מוצגים שני כפתורים קומפקטיים והסבר קצר שמומלץ להתחיל בזמן הקצר.

בנוסף תוקנו כמה שלבים שבהם מבנה הזמנים הישן היה מטעה:
- כרעיים ים־תיכוניות: 55 דקות ואז 20 דקות עם הזיתים, במקום 75 + 20.
- תבשיל שריר: תזכורת אחרי שעתיים להוספת הירקות ואז 45–60 דקות נוספות.
- לזניה בשרית: אפייה מכוסה, השחמה ומנוחה הם עכשיו שלושה שלבים נפרדים.
- חריימה: סיום בכוסברה ומנוחה הם שלבים נפרדים.
- שתי פשטידות: האפייה והמנוחה הופרדו.

### 4. רכיבים שכבר הוכנו

נוסף `step.needs` למצבים שבהם `ingredient_refs` אמנם נכונים טכנית, אבל לא נכונים קוגניטיבית בזמן בישול.

במקום להציג למשל שוב "800 גרם בשר" בזמן הרכבת לזניה, מצב הבישול יכול להציג:
- רוטב הבשר המוכן
- הבשמל המוכן
- דפי לזניה

`needs` יכול להכיל רכיב מוכן או הפניה למצרך רגיל:

```json
"needs": [
  { "label": "רוטב הבשר המוכן" },
  { "label": "הבשמל המוכן" },
  { "ingredient_ref": "lasagna" }
]
```

53 שלבים משתמשים כרגע בתצוגה המשופרת הזו.

## בדיקות

ה־validator עודכן לבדוק גם:
- שכל מתכון כולל זמן מסכם.
- ש־`suggest_timer` מופיע רק עם זמן חוקי.
- ש־`timer_options` תקינים, ללא כפילויות, וכוללים את ברירת המחדל.
- שברירת המחדל בטווח היא הקצה התחתון.
- שכל `ingredient_ref` שמופיע בתוך `needs` קיים במתכון.

מצב נוכחי:

- 25 מתכונים.
- 33 תגיות.
- 350 מצרכים, כולם בשימוש.
- 80 שלבים עם נתוני זמן.
- 39 שלבים עם טיימר מוצע.
- 21 שלבים עם בחירת טווח זמן.
- 53 שלבים עם `needs` מותאם למצב בישול.
- 0 שגיאות validation.

## מה נשאר מההמלצות הקודמות

### טיימר פעיל אחד בלבד

עדיין ניתן להפעיל טיימר אחד בכל רגע. הפעלת טיימר חדש מחליפה את הקודם. זה היה הסעיף החמישי בדוח הקודם ולא שונה ב־v6, בהתאם לבקשה לעדכן כרגע רק את ארבעת הראשונים.
